# ─────────────────────────────────────────────────────────────────────────────
# CNC (Genesis) Node Dependencies
# Core libraries for orchestration, cloud interaction, and task scheduling.
# ─────────────────────────────────────────────────────────────────────────────

# --- Core & Cloud ---
ansible-core
boto3
python-dotenv
pyyaml

# --- Orchestration ---
temporalio

# --- IaC (if used from CNC) ---
# Pulumi, Terraform, etc. would go here.
# For this project, IaC is handled by the CNC node's Docker image,
# which has these tools pre-installed.

# --- Utilities ---
requests
