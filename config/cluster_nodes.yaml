# ─────────────────────────────────────────────────────────────────────────────
# Cluster Node Registry
# Used by scripts/reload_all_nodes.sh and scripts/deploy.sh --remote
#
# Fields:
#   name  - Human-readable identifier
#   host  - IP or hostname (use 'localhost' for the Genesis/CNC node)
#   role  - genesis | control | execution
#   user  - SSH username (optional, defaults to current user)
#   key   - SSH private key path (optional, uses default SSH config if omitted)
# ─────────────────────────────────────────────────────────────────────────────

nodes:
  - name: genesis
    host: localhost
    role: genesis
    project_dir: /home/pi/Projects/ai-orchestration-project

  - name: worker-main
    host: macbook.local
    role: execution
    user: rinehardramos
    project_dir: /Users/rinehardramos/Projects/ai-orchestrator-project
    # key: ~/.ssh/id_rsa   # uncomment if using a specific key

  # Add more nodes as you expand:
  # - name: worker-cloud-1
  #   host: 10.0.0.5
  #   role: execution
  #   user: ubuntu
  #   key: ~/.ssh/cloud_key.pem

  - name: worker-sigbin
    host: 192.168.100.100
    port: 22002
    role: execution
    user: sigbin
    # project_dir: /home/sigbin/ai-orchestration-worker

