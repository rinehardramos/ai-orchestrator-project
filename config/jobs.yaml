jobs:
- id: diagnostic_self_test
  match_string: 'Run diagnostic self-test:'
  type: diagnostic
agent_defaults:
  max_tool_calls: 50
  max_cost_usd: 0.5
  shell_timeout_seconds: 120
  activity_timeout_minutes: 30
