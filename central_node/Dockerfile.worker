FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y gcc && rm -rf /var/lib/apt/lists/*

COPY central_node/worker.py .
COPY src/ /app/src/

RUN pip install google-genai langgraph temporalio pydantic redis boto3 qdrant-client

CMD ["python", "worker.py"]
