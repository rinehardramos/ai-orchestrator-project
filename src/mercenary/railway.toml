[build]
builder = "DOCKERFILE"
dockerfilePath = "src/mercenary/Dockerfile"

[deploy]
startCommand = "uvicorn src.mercenary.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
