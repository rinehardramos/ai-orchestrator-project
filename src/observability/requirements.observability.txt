# Observability Plane dependencies
# Install with: pip install -r requirements.observability.txt

# Metrics
prometheus-client>=0.20.0

# Async HTTP (polling)
httpx>=0.27.0

# Web dashboard
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# WebSocket / Redis pub-sub
websockets>=12.0
redis[asyncio]>=5.2.1

# Docker SDK (local container stats)
docker>=7.0.0

# Temporal (workflow monitoring)
temporalio>=1.7.1

# Config
pyyaml>=6.0.1
